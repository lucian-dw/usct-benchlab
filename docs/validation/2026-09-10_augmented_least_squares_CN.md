# 秩亏与病态子问题：稳定最小二乘求解

## 经典方法的 64/128 阵元初步对照

2026-09-10，使用 `365389307c5f899f5e624b996d78e6ad71033404` 的注册
`straight_cgls`、`straight_sirt`、`straight_sart`、`bent_ray_gn`，完成 HET、NBP-D
各 64/128 阵元共 16 次重建。没有使用有限频率时延 TRF 扩展替代这四种方法。

- 直接复用现有 128 阵元 k-Wave-derived feature case；64 阵元取偶数编号 TX/RX。
  HET 特征为既有 bounded water xcorr，NBP-D 为既有 Hilbert envelope onset。
  本轮没有重新提取、优化或校正特征，也不声称这两种特征已穷尽数据适配可能性。
- 同一 256x256 网格、水初值和 1300--1700 m/s 边界；禁用 GT ROI 更新限制。
- 留出同一批物理通道，排除其互易通道泄漏。HET 共用 342 条，NBP-D 共用 344 条。
  64 与 128 的公共观测、训练/验证掩码已逐元素核对。
- 保持每种算法自身的发布配置。128 的训练 precision 总量归一至 64 基线，
  防止更多观测被动改变 CGLS/Bent 的数据项/正则项比例。
  SIRT/SART 使用行归一化和既有平滑，并不实现同一个 CGLS Laplacian 目标。
- CGLS/SIRT/SART 的迭代上限为 200/400/100，Bent 为 8；每次内部计时上限 180 s，
  外部保护 240 s。4 个并发 CPU 任务，整组墙钟约 297 s；没有运行新 GPU 波场仿真。
  留出数据仅用于选中 checkpoint，不用 GT 选图。

下表为组织区指标，箭头表示 64 到 128。PSNR、停止原因、结果 SHA256 和全部指标
保存在外部运行目录 `classical_array_count_20260910/comparison.csv`、`comparison.json`。

| 样本 | 算法 | RMSE m/s，64 -> 128 | SSIM，64 -> 128 |
|---|---|---|---|
| HET | CGLS | 37.741 -> 39.670 | 0.107 -> 0.092 |
| HET | SIRT | 35.037 -> 35.313 | 0.175 -> 0.176 |
| HET | SART | 35.085 -> 35.766 | 0.180 -> 0.166 |
| HET | Bent | 34.788 -> 39.874 | 0.207 -> 0.098 |
| NBP-D | CGLS | 27.088 -> 27.222 | 0.117 -> 0.113 |
| NBP-D | SIRT | 26.873 -> 26.857 | 0.122 -> 0.120 |
| NBP-D | SART | 26.928 -> 26.961 | 0.123 -> 0.118 |
| NBP-D | Bent | 26.888 -> 26.782 | 0.126 -> 0.127 |

**当前配置下没有明显的纹理恢复收益，但这不是采样上限或模型上限的证明。**
128 Bent 两例因时间预算停止（7 次），64 Bent 完成 8 次；其余均到达迭代上限。
部分选中图像早于最后一次迭代。例如 HET CGLS 选中 48/55 次，64 Bent 选中第 2 次。
CGLS 的选中结果未获得充分的一阶最优性证书；SIRT/SART 平滑及目标权重也不能等同于
CGLS。因此下一项应是对**相同实际离散目标**使用独立参考解核验求解误差，
以及模型匹配数据的数值验收，不是回到时延目标优化或根据图像盲目增加迭代。

现有相关测试在 A100 上复跑 193 项通过，覆盖伴随、Eikonal 导数、增广系统与独立
稠密参考解、秩亏、约束和预算路径。这只是实现验证的一部分，不能替代上述真实规模验收。

## 问题与范围

低 GT 前向误差不能保证反问题可辨识，也不能证明求解器已收敛。
本轮固定观测、训练划分、Jacobian 和正则项，专门检查数值子问题。
不改变物理模型、不通过增加正则强度掩盖数值误差、不使用 GT 选步长或停止。

令模型相对参考的扰动为 $u$，未加权观测残差为 $r=d-F(m)$，
$W$ 是训练数据的 **precision**（不是其平方根），$\lambda$ 是正则项系数。
Gauss--Newton 子问题为

$$
\min_{\delta m}\frac12\|W^{1/2}(J\delta m-r)\|_2^2
+\frac{\lambda}{2}\|L(u+\delta m)\|_2^2.
$$

直接形成的正规矩阵为 $J^*WJ+\lambda L^*L$。即使不显式组装，
CG 仍在这个正规系统上迭代。未正则化且满列秩时，其条件数是加权 Jacobian 条件数的平方；
秩亏时不能套用正定矩阵的唯一解假设。

## 工程实现

新增 `solvers/augmented.py`，复用 SciPy 的 LSMR/LSQR，求解矩形增广系统

$$
M=\begin{bmatrix}W^{1/2}J\\\sqrt{\lambda}L\end{bmatrix},
\qquad h=\begin{bmatrix}W^{1/2}r\\-\sqrt{\lambda}Lu\end{bmatrix},
\qquad \min_{\delta m}\|M\delta m-h\|_2.
$$

- 仅调用已有的 forward/adjoint；不生成稠密 Jacobian 或正规矩阵。
- 复压力的实部、虚部堆叠，未知量仍为实数；伴随使用实参数的正确转置。
- 零权重/留出通道先索引剔除，再计算，避免 `0 * NaN` 污染。
- ROI 使用自由变量限制；ROI 外当前模型对正则残差的贡献仍保留。
- 支持一般矩形 $L$，包括细网格上的 $LB$；不是把粗参数空间 Laplacian 冒充细网格正则。
- 原 `normal_cg` 保留为显式对照。非线性 GN、原生 Bent 的内层默认 LSMR；
  CGLS/SIRT/SART 注册算法和生产 MATLAB FWI 不替换为别的算法。
- 没有隐式添加 ridge。Laplacian 有常数零空间，不能声称“有正则就一定唯一”。

还必须统一整个增广系统的数值尺度。独立测试中，条件数约 $10^6$ 的相同问题，
仅令 $M,h$ 同乘 $10^{-12}$，未归一化的库调用就因条件数估计提前退出，
解的最大绝对误差约 0.74（LSMR）/ 0.80（LSQR）。这不是物理模型误差。
最终采用独立的二进制尺度：先归一化 $h$，再从归一化梯度方向的算子乘积估计 $M$ 的尺度；
求解后显式还原步长尺度，数据与正则行使用同一算子尺度。
不能简单用 $\|h\|/\|M^*h\|$ 放大整个问题，因为 $h$ 中巨大的左零空间分量会导致溢出。
$10^{-12},1,10^{12}$ 三档单位测试均通过，不靠放宽参考解容差解决。
保存 `rhs_binary_exponent`、`operator_binary_exponent`、`step_binary_exponent` 以供复核。

可选 `column_rms` 用固定 seed 的训练增广伴随 Rademacher 探针估计列范数。
右缩放 $\delta m=Dz$ 同时作用于 $J$ 和 $L$，求解 $MDz\simeq h$。
缩放设置正数下限并限制范围，避免低覆盖列出现无穷放大。
这只是预条件，不改变指定目标；但秩亏时最小 $\|z\|$ 和最小 $\|\delta m\|$ 的
解选择不同，因此默认关闭，并记录 `nullspace_selection`。

## 停止与审计

每次内层求解后重新计算 $e=M\delta m-h$，并交叉计算未右缩放的
$M^*(M\delta m-h)$ 和 $M^*M\delta m-M^*h$，保守取两种正规残差范数的较大值：

$$
\epsilon_N=\frac{\|M^*e\|_2}{\|M^*h\|_2}.
$$

`normal_residual_target_met` 表示该值达到 `rtol`。`converged` 还要求 SciPy 的
backward-error 条件成立（停止码 0/1/2/4/5）；条件数或预算截断不宣称收敛。
初始正规右端为零时，零步是子问题驻点。
另行保存 SciPy 的 backward-error 停止码、条件数估计、增广残差、二次目标下降、
实际 forward/adjoint 工作量及时间。条件数是迭代估计，不是秩的证明。
SciPy 的停止条件和物理正规残差阈值不是同一个判据，因此分别报告。

迭代上限、条件数上限、全局时间/算子预算耗尽不能标成收敛。
预算中断时不接纳没有完成检查的内层解；外层保留原有完整检查点。
内层证书只针对无约束线性化子问题，不等于非线性或物理边界约束下的最优性。
外层投影和 Armijo 仍必须检查实际下降。

## 参数

算法 YAML 的 `parameters` 可包含：

```yaml
inner_solver: lsmr
inner_iterations: 64
inner_options:
  rtol: 1.0e-3
  atol: 1.0e-10
  btol: 1.0e-10
  conlim: 1.0e8
  preconditioner: none
  preconditioner_probes: 8
  preconditioner_seed: 0
```

| 参数 | 类型 / 范围 | 含义 |
|---|---|---|
| inner_solver | normal_cg / lsmr / lsqr | 同一 GN 子问题的数值后端 |
| inner_iterations | 正整数 | 工作上限，不是成功标准 |
| rtol | 有限非负 float | 真正规残差相对初始右端的验收阈值，库默认 1e-7 |
| atol / btol | 有限非负 float | SciPy 增广系统 backward-error 容差，默认各 1e-10 |
| conlim | 有限非负 float | 右缩放增广矩阵的条件数估计上限；0 禁用 |
| preconditioner | none / column_rms | 是否估计列范数作右缩放；normal_cg 仅支持 none |
| preconditioner_probes | 正整数 | 列范数估计探针数；工作量计入预算 |
| preconditioner_seed | 非负整数 | 探针固定随机种子 |

研究比较先固定迭代/算子预算，不把更大的工作预算归因于更好的数值方法。
测量或前向求解本身有误差时，也不应追求无意义的机器精度。

## 数值验收

独立测试覆盖 SVD/稠密增广最小二乘参考、秩亏和零空间、实/复数据、
单位缩放、ROI、矩形正则、坏通道、预算及非收敛状态。
实际 k-Wave 检查点对照使用原来的数据、模型与频谱积分点，
不把同模型合成小题的成功称为独立 k-Wave 成像验证。

测试保留独立稠密参考、尺度不变性、秩亏、伴随、预算和真实失败回归；
重复的参数笛卡尔积已精简，不以测试条目数量作为数值可信度指标。
运行 `pytest -q -W error` 检查当前版本，另运行 Black、Ruff、compileall、
CLI、synthetic smoke 和 release audit。可选 CUDA 与外部 FWI 环境可能导致两端跳过数量不同。

HET 与 NBP-D 的 256×256、64 TX/RX、61 频点检查点审计已完成。
第一轮六个独立 GPU 作业使用 `augmented_source_r1`，复核完整目标梯度；
第二轮使用下述 r2 快照进行同点、同目标的内层求解对照。两轮均保留输入和 checkpoint 哈希。
r1 早于最后的成功状态收紧，不能仅使用它的 `converged` 布尔字段判定成功。

最终固定点对照使用 `augmented_source_r2`，包含二进制尺度和双路径正规残差检查。
在同一次线性化中比较 CG、LSMR、LSQR、列缩放 LSMR，工作上限为 64 / 256 / 1024；
较大上限仍允许数值停止条件提前终止。省略重复的非线性有限差分，不宣称重新做过该关卡。
旧审计时原任务尚在更新 checkpoint，因此旧表不能直接和这里的数字作同点比较：

| 样本 | 本次固定 checkpoint SHA-256 |
|---|---|
| NBP-D | `7c4fde8607cf73adbfda46e9a48df27f3ada06acab98147a2b4f9533a3e0ff52` |
| HET | `33d8e0fa9302d0349ec121fb5585e1b0ab7d0e035389235461a988b0a8aa2e5d` |

这些输入只用于数值子问题对照，没有使用 GT 修改它们。

### 同预算结果

下表均为 64 步上限，从零步开始，两个样本分别固定同一 Jacobian、正则和训练通道。
调用数包含本次求解和审计中的 J/J*，不包括共享的背景场构建与数据准备。

| 内层方法 | NBP-D 真正规相对残差 | HET 真正规相对残差 | J + J* 调用 | NBP-D / HET 时间（秒） |
|---|---:|---:|---:|---:|
| CG on normal equations | 6.253e-3 | 4.625e-3 | 132 | 5.52 / 7.87 |
| Augmented LSMR | **1.586e-3** | **1.101e-3** | 135 | 5.73 / 7.44 |
| Augmented LSQR | 4.887e-3 | 4.746e-3 | 135 | 5.72 / 8.07 |
| LSMR + column RMS | 2.415e-3 | 1.386e-3 | 143 | 6.11 / 7.97 |

LSMR 用约 2.3% 更多算子调用，将真正规残差分别降低约 3.94 / 4.20 倍。
但相同 64 步下，LSMR 的二次目标值是 2.773e-14 / 2.436e-13，
CG 是 2.600e-14 / 2.132e-13；**不能因此声称 LSMR 对所有指标都更优**。
LSQR 与 LSMR 关注的迭代残差性质不同，最终应检验同一目标的可信最优解，而非只选一列排名。

| LSMR 上限 | NBP-D 真正规相对残差 | HET 真正规相对残差 | J + J* 调用 |
|---|---:|---:|---:|
| 64 | 1.586e-3 | 1.101e-3 | 135 |
| 256 | 1.503e-4 | 1.200e-4 | 519 |
| 1024 | 9.254e-6 | 6.542e-6 | 2055 |

256/1024 步达到了 1e-3 正规残差目标，但 SciPy 仍返回迭代上限停止码 7；
严格的 `atol=btol=1e-10` 条件尚未全部满足，因此 `converged=False`。
CG 的较大预算运行会在正规残差首次达到 1e-3 后提前结束（D 261 步，HET 231 步），
它的停止规则较弱，不能把 1024 上限误写成 CG 实际也计算了 1024 步。
列 RMS 缩放在这两例和三档预算下均未改善正规残差，仍保持可选、默认关闭。

![Fixed-checkpoint solver audit](../assets/augmented_solver_audit.png)

图上各点是独立工作上限实验，不是同一次求解中保存的逐迭代轨迹。
[完整的 24 项结果摘要](../assets/augmented_solver_audit.csv)包含实际迭代数、算子调用、
二次目标、时间、停止码及 checkpoint 哈希；没有提交原始数据或模型数组。

### 从固定点审计到外层验收

上述固定点对照不包含完整图像反演，不能用其正规残差推算 RMSE/PSNR/SSIM。
固定点的完整目标有限差分：NBP-D 的相对误差为
9.84e-5 / 2.46e-5 / 6.15e-6（步长 1 / 0.5 / 0.25）；
HET 为 7.19e-2 / 1.03e-5 / 2.57e-6。
HET 的较大扰动不符合局部二阶误差趋势，小步长才恢复四倍缩减；
需要继续检查相关峰分支变化、非线性信赖域和实际投影步骤，不能断言失败全部来自内层 CG。

下一步应选择与前向精度相称的内层容差，配合外层下降和留出验证，
而不是把 1024 步固化成新的默认值。稳定子问题只是可信反演的一部分。

### 已完成的 NBP-D 外层对照

后续 `outer_high_d_single_lsmr64_nofilter_r1` 已完成。它采用冻结的
`outer_source_1459a0f`，不是之后的投影回溯、峰导数 QC 或 TV 版本。
两列均使用相同 k-Wave 压力、64 TX/RX、256×256 网格、61 频点组成的单宽带、
水初值、训练/留出接收器和物理 Laplacian；没有 GT ROI。

| 项目 | 先前 CG 对照 | 增广 LSMR 外层对照 |
|---|---:|---:|
| 运行目录 | `high_d_single_dense61` | `outer_high_d_single_lsmr64_nofilter_r1` |
| 内层迭代上限 | 12 | 64 |
| 方向平滑 sigma，pixel | 1 | 0 |
| 外层上限 / 完成迭代 | 15 / 7 | 20 / 20 |
| 停止原因 | `time_budget` | `max_iterations` |
| 组织区 RMSE，m/s | 27.5960 | 21.3361 |
| 组织区 PSNR，dB | 12.7642 | 14.9988 |
| 组织区 SSIM | 0.17989 | 0.26718 |
| 训练相对残差 | 0.410027 | 0.000135286 |
| 留出接收器相对残差 | 0.309040 | 0.111245 |
| 反演阶段时间，秒，不含准备 | 7132.8 | 4328.2 |

这是**求解流程组合对照，不是只替换内层求解器的单因素实验**。
两者上限均为 7200 秒，但实际迭代数、内层工作量和方向平滑不同；
先前 CG 还在已拒绝的线搜索试探上花费时间。不能把全部改善归因于 LSMR。
最终 LSMR 结果选中第 20 次迭代，预算停止不意味着已满足图像质量或最优性目标。
组织轮廓有所恢复，但细节、背景振荡和泛化差距仍然明显，**成像目标尚未验收**。

另外两项同预算 NBP-D 修复对照也已完成：

| 对照 | 组织区 RMSE / PSNR / SSIM | 留出相对残差 | 与原 LSMR 图像的差异 |
|---|---|---:|---|
| 可行位移回溯，`backtracking_r2` | 21.3361 / 14.9988 / 0.26718 | 0.111245 | 逐像素相同 |
| 回溯及峰导数 QC，`peakqc_r3` | 21.3022 / 15.0126 / 0.26772 | 0.110163 | 图像差的 RMS 为 0.1837 m/s |

两项均以 `max_iterations` 停止并选中第 20 次迭代。完整目录前缀为
`outer_high_d_single_lsmr64_`。这说明本例没有靠这两处修复解决剩余模糊；
它们的独立数值回归价值与实际成像增益必须分开陈述。

### 后续受控正则化对照

现有有限频率实验增加了可选的 SciPy TRF/LSMR 信赖域控制和
[平滑各向异性 TV](../algorithms.md#optional-finite-frequency-tv-penalty)。
TV 对照只改变空间项，不改变观测或数据残差；物理梯度的转置和小型有界优化结果
分别与独立差分实现、稠密 L-BFGS-B 参考核对。它不是一个新注册的物理算法。

HET 的二次项/TV 对照均为 20 次接受迭代、7200 秒预算，
观测值、有效掩码、权重、相干度和峰质量数组逐项一致。
TV 转折尺度预先固定为 5 m/s 的水速线性化表示，没有看 GT 调参；
其正则系数与 Laplacian 系数不能解释为已分别最优调参。
下面分别记录最终结果，未完成的其他对照不用于成像结论。

水初值二次项 TRF 的最终选中结果如下，组织指标不含外部水背景：

| 样本 | RMSE m/s / PSNR dB / SSIM | 训练 / 留出相对残差 | 完成/选中迭代 | 停止原因 |
|---|---|---|---|---|
| NBP-D | 21.1118 / 15.0906 / 0.27405 | 0.0001120 / 0.100703 | 20 / 20 | `max_iterations` |
| HET | 31.2163 / 11.8042 / 0.24100 | 0.151643 / 0.149692 | 18 / 18 | `small_model_update` |

目录分别为 `outer_high_d_single_trf64_b5c27d5`、
`outer_low_ob_single_trf64_f5ae7f3`。NBP-D 求解耗时 4528.8 s；相对上述 GN
`peakqc_r3` 只有小幅改善。两例均未解决剩余模糊/伪影。
TRF 终点的缩放最优性指标分别为 1.95e-5、0.25565，均高于设定的 1e-6。
不能把预算停止或更新停滞称为已经达到驻点，也不能据低训练残差宣称成像通过。

停滞点的复查可复用 `--solver-audit-checkpoint`，并指定
`--audit-gradient-steps 0.01 0.001 0.0001 0.00001`。
这些数值乘以一个按当前模型缩放的负梯度方向，该方向最大相对平方慢度变化为
0.002；不是 m/s，也不改变求解器的迭代步长。默认仍为 `1 0.5 0.25`。
小尺度检查完整非线性损失与正则项，逐通道记录误差与无效预测；不只检查
Jacobian/伴随内积，也不将无效训练通道删掉以制造通过结果。
该审计是局部数值诊断，不证明全局最优，亦不使用 GT。

相同原系数下的平滑 TV 对照也已结束：

| 样本 | RMSE m/s / PSNR dB / SSIM | 完成/选中迭代 | 停止原因 |
|---|---|---|---|
| HET | 30.5961 / 11.9785 / 0.24795 | 19 / 19 | `time_budget` |
| NBP-D | 21.7188 / 14.8444 / 0.26838 | 20 / 20 | `max_iterations` |

目录为 `outer_low_ob_single_trf64_tv5_b5c27d5`、
`outer_high_d_single_trf64_tv5_b5c27d5`。HET 只有小幅改善，NBP-D 反而略差；
不能把该设置推广为更优默认，尤其二次惩罚与 TV 的同系数并不表示同等正则强度。
组织区指标来自最终选中图，不是最后一次试探步；HET 仍有明显径向伪影。

HET 因而补充一个固定目标的初值对照，不增加迭代上限：
`outer_low_ob_single_trf64_phaseinit_f48f3c5` 使用既有 `phase_cgls` 初值、
`--prior-reference water --damping-absolute 0.0055148884323715786`。
观测的七个数组、数据哈希、频点、通道索引、物理 Laplacian、先验数组、
停止预算均已与水初值运行逐项核对。相位拟合仅使用训练通道，约 83.53% 通过
既定 QC；初始化工作计入预算。该项已启动，但尚无最终成像结论。
这不是低到高频率 continuation，也没有借助 GT 初始化或筛选通道。

HET 二次项 TRF 第 8 个检查点中，数据项为 7.0721e-11，正则项为 4.9505e-14，
比值约 0.000700；按保存模型独立重算与日志目标一致。目标值比例本身不是梯度或曲率比例，
不能单凭它断言正则化太弱。为检查系数敏感性，二次项与 TV 各补充一组 100 倍系数对照，
其余条件不变；预先规定以留出接收器残差评价，GT 只做事后成像统计。
这不是完整 L-curve 搜索，也不声称系数已最优。

上述 100 倍系数对照的最终组织区结果为：

| HET 空间项 | RMSE m/s / PSNR dB / SSIM | 留出相对残差 | 完成/选中迭代 | 停止原因 |
|---|---|---:|---|---|
| Laplacian ×100 | 28.6656 / 12.5446 / 0.27292 | 0.122843 | 20 / 20 | `max_iterations` |
| Smooth TV ×100 | 30.8529 / 11.9059 / 0.24573 | 0.146382 | 19 / 19 | `small_model_update` |

两者的终点缩放最优性分别为 0.05269、0.17987，尚未满足 1e-6；
图像仍有明显径向伪影。加大 Laplacian 改善了这一次运行，但不能将其归结为
单纯的平滑效果或认定全局最优，因为非线性选峰也会改变优化轨迹。
已用 `result.h5` 独立重算指标，并确认对应选中检查点，不使用试探步代替结果。

对原 HET 第 18 次停滞点的细尺度审计使用
`plateau_low_ob_trf_gradient_b7f2878`；checkpoint 哈希为
`03b9a8eb6884bc1d3d2dca7fe1f7b0d56eddc79eab3c55577046e46b98ca4094`。
首个 0.01 倍差分方向检查已经出现约 7886.8 的相对导数误差：通道 `(6, 48)`
在正扰动时由 0.116 us 跳至 11.522 us，线性化只预测约 -0.000312 us 的变化，
而负扰动仍停留在原峰附近。该步没有无效训练通道被丢弃。
这确认了实际计算的选峰输出在该扰动区间发生跳变，但仍需区分真实竞争峰、
离散选峰和传播精度的影响，不能直接声称局部导数公式错误。
更小扰动审计现已完成：0.0001、0.00001 倍方向的相对误差分别为
1.11e-10、9.24e-10，没有丢弃无效训练通道。局部解析导数因此得到支持，
但不能跨过选峰跳变来使用。后续精确平移反例确认旧实现按采样高度排序会选错
连续最大峰；数值修复见 [多频段报告](2026-09-09_multiband_traveltime_CN.md)。

此外，现有 `--audit-only` 同时计算 $F(m_{GT})$ 和
$F(m_0)+J(m_0)(m_{GT}-m_0)$，其中 $m_0$ 为水背景。
后者用于检验初始线性化范围，不用它修正观测或初始化反演。
`forward_at_gt.npz` 保存两种预测，`summary.json` 分别报告对 k-Wave 观测及二者之间的差异。
这样可以区分完整前向已匹配与初始线性化已足够准确，避免混为一谈。

### 完整前向、初始线性化与正则化的分开验收

冻结源码 `2e05531` 的两项前向审计已完成：256×256 传播、8 TX × 64 RX、
61 频点、三个重叠子带。它们是通道子集上的归因，不是新的完整重建，
也不能替代全部 64 TX 或未测试样本的验收。

| 样本 | 完整前向训练相对残差 | 水背景一次线性化训练相对残差 | 完整前向留出残差 | 一次线性化留出残差 |
|---|---:|---:|---:|---:|
| NBP-D | 0.004983 | 0.368661 | 0.005389 | 0.101929 |
| HET | 0.002132 | 0.304246 | 0.001909 | 0.398401 |

三子带完整前向 RMSE 分别为 NBP-D 1.228 / 1.035 / 0.907 ns，
HET 1.700 / 3.257 / 4.868 ns。与之相比，一次线性化的有效通道绝对误差中位数
分别为 10.216 ns、51.061 ns；误差最大的约 1% 通道贡献了约 88% 的平方误差。
这说明线性化误差并非均匀分布，**不据此把这些通道按 GT 筛除**。
完整前向在 GT 上拟合良好，并不能保证从水背景出发的局部线性模型有效范围足够大，
更不能保证非线性峰选择目标容易优化。

运行目录为 `tangent_audit_high_d_61_2e05531` 和
`tangent_audit_low_ob_61_2e05531`；其 `summary.json`、`forward_at_gt.npz`
保留预测、有效掩码及分组残差。GT 仅用于前向归因，不进入更新或观测修正。

还对已完成的 64 TX/RX LSMR 运行独立重算了二次目标，采用各自保存的
`config.yaml`、`initial_model.npz`、`checkpoint.npz`，而非重新设定正则系数：

| 样本 | GT 的 Laplacian 正则项 | 选中重建的正则项 | 选中重建的总目标 |
|---|---:|---:|---:|
| NBP-D | 8.54409e-14 | 1.86324e-16 | 1.87183e-16 |
| HET | 2.71450e-13 | 2.30293e-13 | 3.95009e-10 |

其中数据项为非负数，因此 NBP-D 的 GT 总目标下界就超过当前重建总目标约
**456 倍**。这不意味着当前模型已达全局最优，却证明该参数下的目标函数不会偏好
精确 GT；单纯把内层正规残差降得更低，不能消除这种正则化偏好。
HET 没有同样的下界证据，不能把 NBP-D 的原因直接推广过去。
两例网格尺寸都是 256×256，但物理 spacing 不同；表中目标值不可跨样本直接排名。

对应审计保存在运行根目录的 `objective_regularization_audit.json`，包含输入哈希、
选中迭代及目标重算校验。NBP-D 补充的 `outer_high_d_single_trf64_tv5_b5c27d5`
只复用已有 TV 5 m/s 设置、原系数和 TRF 预算，不新增参数扫描。
该设置此前已在 HET 对照中固定；本例用于开发验证而非盲测，最终结果见上表。

HET 的 `outer_low_ob_single_lsmr64_nofilter_r1` 也已结束，选中第 7 次迭代，
以 7200 秒 `time_budget` 停止：组织 RMSE 32.6853 m/s、PSNR 11.4048 dB、
SSIM 0.20247，训练/留出残差 0.367671 / 0.272805。它没有解决射线状伪影，
不得用 NBP-D 的改善代替 HET 的验收。

HET 的 `backtracking_r2`、`peakqc_r3` 随后也均因时间预算停止，完成 8 次接受迭代，
但依据留出残差恢复到第 4 次迭代。最终组织指标分别为
35.6487 / 10.6509 / 0.21571 和 35.6477 / 10.6512 / 0.21575
（RMSE m/s / PSNR dB / SSIM），留出残差分别为 0.377874、0.377969。
**不能继续用最后一个中间检查点代替这个选中结果，也不能将修复称为成像改善。**

修复后的三频带对照使用 `9857ec4`，目录为
`outer_high_d_multi_trf64_fixed_9857ec4` 和 `outer_low_ob_multi_trf64_fixed_9857ec4`。
它们保留单宽带 TRF 的绝对正则系数，分别为 0.0004646243842888721、
0.0055148884323715786；物理 Laplacian、公共有效通道、每对通道总权重、
频点和通道坐标已逐项核对。这样不会一边增加观测频带、一边隐式改变正则强度。
两项已完成并从 `result.h5` 独立重算组织指标，且确认与选中检查点一致：

| 样本 | 组织 RMSE / PSNR / SSIM | 训练 / 验证相对残差 | 完成 / 选中迭代 | 停止原因 |
|---|---|---|---|---|
| HET，三子带 | 31.2587 / 11.7924 / 0.25472 | 0.327738 / 0.274300 | 16 / 16 | `small_model_update` |
| NBP-D，三子带 | 25.9529 / 13.2974 / 0.17001 | 0.329321 / 0.221153 | 13 / 13 | `small_model_update` |

没有得到稳定成像收益，NBP-D 明显弱于单宽带；不能据此认定多频信息无用。
二者仍使用旧采样峰排序，会受到后来确认的候选峰排序错误影响；它们不是
低到高频率 continuation，也不是独立频率盲测。

原始水参考的补充频谱检查只记录线索，不改变当前求解对照：HET 的 80--250 kHz
窗口包含约 81.55% 的水信号能量，250 kHz 以上约 18.31%，截至 350 kHz 累计
约 99.91%；NBP-D 的 200--800 kHz 已包含约 99.26%。计算使用实际采样间隔、
单边 FFT Parseval 权重，并对每条非近邻水通道归一化后取平均。
**能量比例不等于可恢复图像信息比例**，这既不能证明扩大频带必然有用，
也不能将 HET 的缺失频段当作 NBP-D 模糊的共同解释。更高频率还需独立离散误差检查。

## 文献与实现依据

- Fong and Saunders, LSMR (2011):
  [论文](https://arxiv.org/abs/1006.0758)，
  [Stanford SOL 实现与预条件说明](https://web.stanford.edu/group/SOL/software/lsmr/)。
- [SciPy LSMR](https://docs.scipy.org/doc/scipy/reference/generated/scipy.sparse.linalg.lsmr.html)：
  任意矩形系统、停止码、backward-error 容差；已有依赖，不引入新的求解器框架。
- Paige and Saunders, LSQR (1982)，及
  [PETSc KSPLSQR](https://petsc.org/release/manualpages/KSP/KSPLSQR/)：
  同时监测数据残差和正规残差，明确预条件后的残差解释。

Golub--Kahan 方法改善数值行为，但不消除物理病态性；
rank-deficient 方程可被稳定求解，并不意味着原图可以唯一恢复。

一个最小反例是 $M=\operatorname{diag}(1,10^{-6})$、$h=M(1,1)^T$。
旧 CG 只按正规残差停止时，一步返回约 $(1,10^{-12})$，
正规相对残差已约 $10^{-12}$，但第二个参数几乎完全错误。
增广 LSMR/LSQR 在相同目标上同时检查 backward error，三步恢复 $(1,1)$。
这是可复现的停止策略差异，不是新物理模型的优势；对应回归测试保留在仓库中。
