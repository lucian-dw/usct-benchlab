# 64 TX / 64 RX 射线算法开发：本次执行报告

日期：2026-09-08。接续 `work/physics-agent-validation`，起点 `78dea0254e6e4eff9977e2be78f898f7c135f3df`。

以下保留该日期对应的历史执行结果；测试计数、运行状态和后续工作边界均属于当时源码，不表示当前工作树已重新验证。恢复重放及其验收门另见[完整重放记录](2026-09-08_64_replay_complete.md)。

## 结论

本次完成了数据校验、四种完整基线复现、真实数值测试、实现修改及分阶段推送。可以确认的工程收益是 **Siddon 等价稀疏加速**和**非有限预测的失败状态修复**。新增的 **32² 慢度参数化**对高频 D 的 CGLS/Bent 及低频 OpenBreastUS 的 CGLS 有改善，但不稳定推广；SIRT/SART 没有取得稳定成像提升，Bent 在低频 OpenBreastUS 明显退步。**因此默认成像参数和特征未改成实验配置，不能宣称四种算法都获得了普遍质量提升。**

实验包含三组采集、两种参数化、四种方法，共 24 个结果，以及高频 D 的四个相位负对照，共 28 个主比较结果。高低频 D 是同一解剖图的两种采集，故只有两张不同解剖图，绝不是 28 个独立样本或盲测。复现、CSR 等价性与最终复跑的额外运行保留在过程日志，不混入样本统计。

## 1. 数据、环境和基线

已读取附件要求、README_CN.md、evaluation_contract.md、baselines_64/summary.json，以及指定源码 README、评估/物理文档和相关调用路径。没有把有限路径检查称为全仓逐行审计。

解压可执行，Python 3.13.5、NumPy 2.3.5、SciPy 1.17.0、Numba 0.65.1 可用。本机为 CPU 环境，容器约 4 核/4 GiB 限额；没有在 A100、MATLAB 或临床数据上运行。在线 pip 和命令行 Git 因 DNS 失败；使用已有对应版本的 Black/Ruff wheels 和已安装的数值依赖完成离线 editable 安装，GitHub connector 支持真实读写，已逐批快进推送，不强推、不合并 main、不创建远端新分支。

`tools/verify.py --repo usct-benchlab` 验证了 **61 个文件哈希和 12 个 HDF5 的数组哈希/schema**。原数据文件未改动。源码快照恢复其执行位后，tree 为 `603d496bf533f7a1ac6448842b7f79761a6f98f2`，与指定基线相同。第一次测试的两个 release 测试依赖 `git ls-files`，因 zip 没有 `.git` 索引而发生环境失败；补齐本地索引后，未改数值源码的结果是 **161 passed、2 skipped**，不是把初次失败隐去。两项跳过的是未执行的外部 MATLAB 测试。

高频 D 的 raw object/water 均为 `(6381,64,64)` float32，轴序 `(time,tx,rx)`。实际时间轴及 `dt*sum(p(t)*exp(+i*2*pi*f*t))` 被直接使用；重新计算全部存储频点，与包内复压力的相对误差为 object `1.8141e-17`、water `1.2373e-18`。这验证转换一致性，不是对 k-Wave 物理精度的独立证明。低频没有完整 raw；本次未声称在其上重做时域 picker。

所有输入是父 128 通道模拟的偶数索引观测子集，不是新 64 通道模拟。仍是 property maps 自仿真的 k-Wave 压力，不是临床测量。派生案例明确标记 `uses_gt_generated_measurement=true`。

先完整执行 CGLS/SIRT，再 SART/Bent，未使用 `--quick`。与附件 baselines_64 的保存数组比较，直线三方法最大绝对差 `4.548e-13 m/s`，SSIM 差异小于 `1.5e-12`；Bent 逐元素完全相同；停止原因、选中/完成迭代和划分相同。这里只确认基线复现，不构成成像质量提升。原高频结果为：

| 方法 | 组织 RMSE (m/s) | 组织 SSIM | selected/completed | 停止原因 |
| --- | ---: | ---: | --- | --- |
| CGLS | 27.193208708966395 | 0.114274175695250 | 120/120 | max_iterations |
| SIRT | 27.005598960685713 | 0.121096114777781 | 200/200 | max_iterations |
| SART | 26.927737346553016 | 0.122694304335006 | 100/100 | max_iterations |
| Bent | 27.087771351 | 0.130122492 | 19/20 | max_iterations |

最终失败状态修复后再跑上述四种配置，仍保持这一等价性；32² 四种最终数组也与此前同配置实验逐元素完全相同。见过程文件 `final_array_equivalence.json`。

## 2. 本次实现与数学边界

### 2.1 Siddon 等价 CSR：默认开启的执行优化

原交点和长度系数不变，缓存 CSR 并以同一矩阵的转置实现伴随；保留 `projector_backend: reference`。行/列平方范数先合并同一像素的重复系数再平方，匹配真实矩阵范数。前向与伴随仍在各自命名空间内，没有把重建求解器叫作伴随。

完整 64 通道的成对 CPU 求解循环耗时为 CGLS **2.423 → 0.709 s**、SIRT **4.544 → 0.892 s**、SART **13.749 → 2.656 s**。这些是本机单次成对观察，约 3.4/5.1/5.2 倍循环加速，不是包括所有 setup 的端到端倍率，也不是 A100 测量或统计性能保证。额外 CSR 存储 12,956,968 字节，同时仍保留原交点列表；并非零额外内存。重建数组和停止条件不变，**这不是成像质量收益**。

### 2.2 可选 32² 参数化：改变模型空间，不改变传播网格

令 `delta_s = B a`。B 为非负、保常数、边界复制的双线性基，a 在 32² 网格上，实际声速输出、Siddon 路径和 Eikonal 传播仍在 256² 网格。直线前向/伴随为 `A B` 与 `B^T A^T`；Bent 每次在当前细网格模型重新计算真 Eikonal，其局部线性化为 `J B` 与 `B^T J^T`。不是降采样采集，不是锐化结果，更不是把全波方法改名。

正则保持原细网格的 `L B a`，梯度为 `B^T L^T L B a`，lambda 没有因为参数网格变化而偷偷改量纲。系数慢度满足盒约束，B 的非负与行和为 1 保证细网格慢度也在盒约束内。以 water 配置初始化；不由 GT 或 GT mask 初始化、拟合 offset 或决定更新区。

仅试了预先记录的一种 `[32,32]` 选择，没有巨大网格/正则 sweep；选择动机是减少相对训练路径数过多的未知量，并非由 GT 搜索最优格点数。它是一种显式空间先验，不能凭先验补回真实细节。粗模型暂不支持反演 ROI；粗 Bent 暂不支持非均匀初始化或 CGLS warm start，遇到不支持组合明确报错。

工程近似：原有迭代内 Gaussian 宽度按网格间距换算，名义物理宽度保持，但离散滤波器并不完全等价，粗网格下可能接近恒等。因此这里比较的是完整的模型空间方案，而非声称只改变未知数个数、其余离散先验严格相同。没有在输出图像上添加滤波或锐化。

### 2.3 相位特征：保留负对照，不替换默认

该负对照使用未修改的高频 D510022534 采集及 `78dea025` 基线加等价 CSR 执行变化，没有重跑 k-Wave。频率/阈值政策和完整原始求解器配置在首次运行前冻结；四种重建均在当时的 CPU 会话完成，返回数组及停止历史全部保存。

使用源频率 500 kHz 的水参考相位，`tau=(arg(P/Pwater)+2*pi*k)/(2*pi*f)`；原 pulse delay 只决定整数周期 k。预处理逐通道，不使用 GT，不做互易平均、跨通道拟合或验证数据驱动调参。原 2,752 个有效通道、权重和划分全部保留。此处相位除频率不是相位的频率导数或 group delay，仍是几何射线到时近似，不是有限频率散射反演。

以下均为使用公共组织 mask 和固定 full-GT 范围的事后组织指标：

| 方法 | 原 RMSE (m/s) | 相位 RMSE (m/s) | 原 SSIM | 相位 SSIM | 相位 selected/completed | 相位停止原因 |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| CGLS | 27.193209 | 30.256734 | 0.114274 | 0.082443 | 120/120 | max_iterations |
| SIRT | 27.005599 | 29.321818 | 0.121096 | 0.098822 | 200/200 | max_iterations |
| SART | 26.927737 | 29.288975 | 0.122694 | 0.099221 | 100/100 | max_iterations |
| Bent | 27.087771 | 29.526167 | 0.130122 | 0.100046 | 7/12 | validation_plateau |

**四种完整实验均恶化，不能用该负对照替换默认特征。** 相位与 pulse-delay 残差的观测定义不同，不能作为相同数据保真项直接排名。没有重跑低频 raw picker，也没有执行低频相位重建；工具保留为 opt-in、可复现的负对照。单频相位解周仍依赖锚点误差小于半周期的假设，不能声称消除了 multipath 或到时偏差。

当时新增的 9 项相位回归覆盖非零时间原点的已知延迟脉冲、整数周期选择、幅度单位不变性、通道局部隔离、无效/缺失数据和半周期歧义。采用时间相量 `exp(-i omega t)`，频谱由 `dt * sum(p(t) exp(+i omega t))` 形成。

### 2.4 修复“失败后仍显示成功”的实际 bug

先注入非有限预测：四方法在第一次或第二次调用出错，共 8 个测试，均复现了不正确状态。原实现可能恢复有限图像却报告 success；Bent 的非有限线搜索预测还可能被误报为线搜索未改善。

修复后，活动通道上的非有限 Eikonal 预测触发 numerical_failure；numerical_failure/linear_solver_breakdown 标为 failed，保留有限回退图仅用于诊断，并写 `recovered_checkpoint_only=true`。没有把回退的初始水图冒充成功反演。8 个故障测试全部通过。

## 3. 固定政策下的成像结果

以下 RMSE 单位 m/s，PSNR 单位 dB。每一组均使用相同组织评价 mask 与固定 full-GT range，SSIM 只统计完整组织窗口；图像保留水背景。表中“原”指本次复现的像素参数化，不是旧 128 通道结果。验证选择迭代不看 GT；GT 仅后评价。GT mask 排除连通到边界的水域，不排除组织内部恰等于水声速的像素。

### 高频 NBP D

| 方法 | 组织 RMSE：原 → 32² | 组织 PSNR：原 → 32² | 组织 SSIM：原 → 32² | 水区 RMSE：原 → 32² |
| --- | ---: | ---: | ---: | ---: |
| CGLS | 27.19321 → 26.43573 | 12.89192 → 13.13731 | 0.114274 → 0.129088 | 3.93543 → 3.24542 |
| SIRT | 27.00560 → 26.80814 | 12.95206 → 13.01580 | 0.121096 → 0.121355 | 2.98990 → 3.52610 |
| SART | 26.92774 → 27.08227 | 12.97714 → 12.92743 | 0.122694 → 0.115192 | 2.84870 → 4.21903 |
| Bent | 27.08777 → 26.37242 | 12.92567 → 13.15813 | 0.130122 → 0.138723 | 4.91255 → 4.12136 |

CGLS 和 Bent 有局部改善；SIRT 的 SSIM 几乎不变且水区变差；SART 的组织和水区都变差。不能只报道正项。

### 低频 NBP D（同一解剖图，另一采集带宽）

| 方法 | 组织 RMSE：原 → 32² | 组织 PSNR：原 → 32² | 组织 SSIM：原 → 32² | 水区 RMSE：原 → 32² |
| --- | ---: | ---: | ---: | ---: |
| CGLS | 32.32602 → 32.29417 | 11.39009 → 11.39865 | 0.083937 → 0.083500 | 4.95915 → 4.63178 |
| SIRT | 31.45263 → 31.98313 | 11.62799 → 11.48271 | 0.092992 → 0.086559 | 4.30615 → 4.80420 |
| SART | 31.45345 → 32.35527 | 11.62777 → 11.38223 | 0.093268 → 0.081954 | 4.25900 → 5.12386 |
| Bent | 33.91362 → 33.43087 | 10.97365 → 11.09818 | 0.068984 → 0.071808 | 6.23772 → 5.28864 |

CGLS 的 RMSE 小幅改善但 SSIM 略降；SIRT/SART 退步，Bent 有小幅改善。不能把高低频差异当作算法提升。

### 低频 OpenBreastUS class 1

| 方法 | 组织 RMSE：原 → 32² | 组织 PSNR：原 → 32² | 组织 SSIM：原 → 32² | 水区 RMSE：原 → 32² |
| --- | ---: | ---: | ---: | ---: |
| CGLS | 37.74073 → 35.01556 | 10.15561 → 10.80659 | 0.107020 → 0.161177 | 10.41189 → 6.81024 |
| SIRT | 35.00465 → 35.17155 | 10.80930 → 10.76798 | 0.179460 → 0.165519 | 5.65827 → 5.79733 |
| SART | 35.08502 → 35.18207 | 10.78938 → 10.76539 | 0.180092 → 0.170952 | 5.80866 → 5.81277 |
| Bent | 35.46875 → 39.23932 | 10.69490 → 9.81738 | 0.174980 → 0.091591 | 9.50469 → 10.85874 |

CGLS 的条纹与水区误差有所下降；SIRT/SART 没有改善，Bent 的 RMSE 与 SSIM 明显退步。**32² 不适合作为所有射线方法的统一默认配置。** 统一灰度图仍然显示细节缺失和伪影，未达到纹理完整恢复。

## 4. 验证、停止、预算：不能用单一分数替代

使用包内 receiver_fraction=0.125、seed=42，排除 reciprocal training counterpart。高频训练 2,098 条、接收验证 344 条、为防互易泄漏额外不用 310 条；有效观测总计 2,752。验证接收编号 `[5,26,38,44,53,60,62,63]`。所有高频变体、方法的划分一致；低频各组内也已自动断言一致。验证集已经用于早停和选迭代，所以不是独立 test。

沿用原上限 CGLS120、SIRT200、SART100、Bent20 outer/inner24，600 s 预算、残差目标 0.01 及原 plateau/update 阈值。没有为了获胜放宽预算或选 GT 最优迭代。高频 32² 的停止为：

| 方法 | 选中/完成 | 原因 | logical forward / adjoint |
| --- | --- | --- | --- |
| CGLS | 22/26 | validation_plateau | 27 / 26 |
| SIRT | 200/200 | max_iterations | 201 / 201 |
| SART | 100/100 | max_iterations | 801 / 808 |
| Bent | 3/8 | validation_plateau | 201 / 200 |

所有主比较记录的停止均为 max_iterations 或 validation_plateau，没有达到残差目标；正常算法返回成功只意味着完成了配置流程，不意味着达到指定成像质量。CGLS step、SART sweep、Bent outer 不能直接横比。Bent 的 logical forward 201 包含 1 次初始非线性前向、8 次线搜索和192次 Jv，而不是201次完整多源传播。逐源 marching 次数/FLOP 尚未额外统一计量，本轮保留了 ledger 的真实粒度，不伪造“等计算量”。

验证残差也不是 SOS 质量的充分代理。例如低频 D 的 SART 验证残差从 0.128355 降到 0.099147，但组织 RMSE 从31.45345升至32.35527。高频 Bent 的组织指标改善时，验证残差反而从0.235050略升到0.238136。相位与包络 ToF 本身又不是同一个观测目标，二者残差不用于横向排名，更不与压力残差直接排名。

完整28行 metrics.csv/metrics.json包含组织、full_image、水区RMSE/bias、训练/验证相对残差、selected/completed、stop_reason、时间和算子工作量；每个 result 附带完整 iteration_history。24张迭代/时间曲线保留两种参数化的 train/validation 与选中迭代，另有3张含 GT 的统一灰度比较图。

## 5. 数学核验与尚存模型误差

独立运行的数值审计中，Siddon 内积相对误差 `7.13e-16`，Eikonal `4.63e-16`，复合 AB `0`，JB `3.33e-16`；非线性 Eikonal 中央方向导数相对误差 `2.83e-9`。正则 LB 的伴随和目标函数梯度也通过单元测试。均匀水经既有校准后误差约 `3.39e-21 s`，但这只验证校准一致性。

平界面独立 Snell/Fermat 到时对照：41²、81²、161² 网格相对误差依次 **2.309%、1.511%、0.940%**，随加密下降；没有宣称一阶 marching 已无离散误差，也没有把通过伴随测试等同于物理准确。

最后的 GT 前向诊断未用于反演、选迭代或校正数据：在高频原包络 ToF 的2,752有效通道上，GT经Siddon与Eikonal前向后的非加权到时RMS误差分别 **0.08457 μs、0.08742 μs**，仍显著不为零。此误差混合了有限频带到时特征、几何物理近似与离散误差，不能据此确定单一原因。但证据不支持“只是迭代不够”的解释。

## 6. 测试与交付

本次最终全套测试：**199 passed、2 skipped**。相比起点新增38项测试；两项外部MATLAB依赖测试仍跳过，没有冒充完成。Black、Ruff、compile、smoke、release audit 均实际运行通过。故障注入修复前失败日志和修复后成功日志同时保留。最终源码树与每次API上传树逐次比对，非本地/远端混用的测试声明。

源码、可应用patch、运行结果、日志与图表见交付包。原始采集数据和字体文件不在交付源码或GitHub提交中。各实验实际执行时间取自日志，主比较统计不使用历史测试计数。CI状态另记录于交付时的状态文件，不把“已触发”当“已通过”。

复现入口：`bash scripts/run_64_validation.sh /path/usct_handoff_64 /path/fresh_output`。需要当前源码安装及包内数据、configs/tools；输出放新目录，原数据仅通过symlink读取。该shell包装器做语法检查，内部Python入口及同等28组命令已经逐项实际执行；没有额外重复整套运行来测试一次包装器。A100机器可运行这条CPU路径，但脚本不会伪称利用GPU。更完整命令见交付的REPRODUCE.md。

## 7. 未完成与下一轮边界

未取得四方法共同、稳定的组织质量改善；尤其SIRT/SART没有达到这一目标，Bent推广失败不能隐藏。未改高阶Eikonal、实现新的有限频率几何核或新的时域picker；相位方案是失败的有限消融，不是物理模型升级。

低频仅执行NBP D与OpenBreastUS class1两例，剩余六例未跑；完整128通道、额外频率版本、生产FWI/Full Green、MATLAB、A100性能与临床实测均未运行。本轮无临床结论、无统计推广保证。所有开发图的GT可见，故不能包装成盲测。

需要进一步定位arrival偏差时，应在低频补充与对应case几何/波形一致的 object/water 原始 `(time,tx,rx)` float32、实际time_axis、源波形/时间零点、频谱约定、原通道索引映射、仿真网格/密度/衰减/PML信息，并校验已有复压力。现有高频raw不应重复索取；低频不应通过GT合成一条“替代原始观测”。下一轮需预先冻结有效通道/验证划分和预算，再决定是否检验到时模型或Eikonal精度，不能继续无依据地减正则加迭代。
