# Experimental Scripts

Scripts in this directory are optional integration or diagnostic entry points.
They are not part of the v0.1 release smoke path.

The external MATLAB adapter scripts require MATLAB and third-party repositories
installed outside this Git checkout. The first-party MATLAB adapter templates
under `scripts/matlab_adapters/` are still kept because unit tests verify the
adapter contract and skip behavior.
