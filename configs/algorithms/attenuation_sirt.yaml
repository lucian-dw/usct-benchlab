name: attenuation_sirt
parameters:
  required_feature_channel: apparent_tof
  iterations: 50
  relaxation: 0.8
  attenuation_upper_np_per_m: 80.0
  min_ray_weight: 0.0
  ray_weight_power: 1.0
metadata:
  required_feature_channel: apparent_tof
  observable_definition: log-amplitude ratio co-extracted from the apparent_tof feature case
  description: Straight-ray attenuation SIRT using feature-derived ray weights without hard thresholding by default.
